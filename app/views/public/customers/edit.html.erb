<h2>編集</h2>
<%= form_with model: @customer, url: public_customer_path(@customer.id) do |f| %>
  <label for = "inputName">名前</label>
  <%= f.text_field :first_name, autofocus: true, id:"inputName", placeholder:"名前" %>
  <%= f.submit "保存"%>
<% end %>
<%= link_to "退会確認画面", confirm_with_path %>